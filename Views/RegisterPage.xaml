<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinanzasApp.Views.RegisterPage"
             Shell.NavBarIsVisible="False">

    <Grid IgnoreSafeArea="True">
        <Grid InputTransparent="True">
            <Ellipse x:Name="TopGlow"
                     WidthRequest="300"
                     HeightRequest="300"
                     HorizontalOptions="Start"
                     VerticalOptions="Start"
                     Margin="-60,-40,0,0"
                     Opacity="0.45">
                <Ellipse.Fill>
                    <RadialGradientBrush Center="0.3,0.2" Radius="0.9">
                        <GradientStop Color="#57AEFF" Offset="0.0" />
                        <GradientStop Color="#00000000" Offset="1.0" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <Ellipse x:Name="BottomGlow"
                     WidthRequest="360"
                     HeightRequest="360"
                     HorizontalOptions="End"
                     VerticalOptions="End"
                     Margin="0,0,-80,-120"
                     Opacity="0.5">
                <Ellipse.Fill>
                    <RadialGradientBrush Center="0.7,0.8" Radius="1.0">
                        <GradientStop Color="#8B5CF6" Offset="0.0" />
                        <GradientStop Color="#00000000" Offset="1.0" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
        </Grid>

        <ScrollView VerticalScrollBarVisibility="Never">
            <VerticalStackLayout x:Name="RegisterContainer"
                                 Padding="26,48"
                                 Spacing="22"
                                 VerticalOptions="Center">
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                    <Label Text="FinanzasApp" Style="{StaticResource TitleLabelStyle}" />
                    <Label Text="Crear cuenta" Style="{StaticResource SubTitleLabelStyle}" />
                </VerticalStackLayout>

                <Border Style="{StaticResource CardBorderStyle}" Padding="20,22" StrokeThickness="0.6" Stroke="#1F2A44">
                    <VerticalStackLayout Spacing="14">
                        <Border Style="{StaticResource InputBorderStyle}">
                            <Grid ColumnDefinitions="40,*" HeightRequest="52" VerticalOptions="Center">
                                <Label Text="👤" HorizontalOptions="Center" VerticalOptions="Center" />
                                <Entry x:Name="UsernameEntry" Grid.Column="1" Placeholder="Usuario" Style="{StaticResource InputEntryStyle}" Focused="OnInputFocused" Unfocused="OnInputUnfocused" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource InputBorderStyle}">
                            <Grid ColumnDefinitions="40,*" HeightRequest="52" VerticalOptions="Center">
                                <Label Text="🔒" HorizontalOptions="Center" VerticalOptions="Center" />
                                <Entry x:Name="PasswordEntry" Grid.Column="1" Placeholder="Contraseña" IsPassword="True" Style="{StaticResource InputEntryStyle}" Focused="OnInputFocused" Unfocused="OnInputUnfocused" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource InputBorderStyle}">
                            <Grid ColumnDefinitions="40,*" HeightRequest="52" VerticalOptions="Center">
                                <Label Text="✅" HorizontalOptions="Center" VerticalOptions="Center" />
                                <Entry x:Name="ConfirmEntry" Grid.Column="1" Placeholder="Confirmar contraseña" IsPassword="True" Style="{StaticResource InputEntryStyle}" Focused="OnInputFocused" Unfocused="OnInputUnfocused" />
                            </Grid>
                        </Border>

                        <Button Text="Registrar" Style="{StaticResource PrimaryButtonStyle}" Clicked="OnRegisterClicked" />
                        <Button Text="Volver al inicio" Style="{StaticResource SecondaryButtonStyle}" Clicked="OnBackClicked" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
